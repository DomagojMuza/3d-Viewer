services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data/db:/app/data/db
      - ./data/uploads:/app/data/uploads
    environment:
      - PORT=${PORT:-3000}
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ORIGIN=${CORS_ORIGIN}
    restart: unless-stopped
