services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data/db:/app/data/db
      - ./data/uploads:/app/data/uploads
    env_file:
      - .env
    restart: unless-stopped
